/**
 * `1sa` — Sambatra's own npm module (CommonJS, untyped).
 * Spells a number out in Malagasy. Installed here so the console can run
 * it for real instead of showing a recorded transcript.
 */
declare module "1sa" {
  export function isa(num: number): string;
}
